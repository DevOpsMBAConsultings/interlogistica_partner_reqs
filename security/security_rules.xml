<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Regla para restringir contactos al comercial asignado (o sin asignar) -->
        <record id="res_partner_rule_salesman" model="ir.rule">
            <field name="name">Contactos: Comercial solo ve sus clientes o sin asignar</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="domain_force">['|', ('user_id', '=', user.id), ('user_id', '=', False)]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <!-- Regla para que Administradores y "Ver todos los documentos" puedan ver TODOS los contactos -->
        <record id="res_partner_rule_salesman_all_leads" model="ir.rule">
            <field name="name">Contactos: Administradores de Ventas ven todos</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman_all_leads'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo>
